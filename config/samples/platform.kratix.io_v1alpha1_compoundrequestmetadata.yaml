apiVersion: platform.kratix.io.kratix.io/v1alpha1
kind: CompoundRequestMetadata
metadata:
  labels:
    app.kubernetes.io/name: kratix
    app.kubernetes.io/managed-by: kustomize
  name: compoundrequestmetadata-sample
spec:
  # TODO(user): Add fields here
