apiVersion: platform.kratix.io/v1alpha1
kind: Cluster
metadata:
  labels:
    environment: dev
    pci-compliant: "true"
  name: dev-3
  namespace: default
spec:
  bucketPath: worker-cluster-1
  id: worker-cluster-1-id
